# 유닛 보관 · 3합 승급 · 드래그 배치 (2026-09-11, 진행 중)

브랜치 `work/2026-09-11-unit-roster` (main에서 분기). 아직 커밋하지 않았다.

## 왜 하는가

사용자 요청으로 코어 루프를 바꾼다. 스핀 결과가 전장에 바로 내려앉던 방식을 폐기하고,
유닛을 보관함에 쌓았다가 드래그로 배치하며 같은 유닛 3기를 모으면 승급하는 구조로 간다.

기획 이탈이 아니다. GDD 「13 정직한 리스크 · 치명」이 스스로 적어둔 탈출구다.

> "릴 좌표 = 전장 좌표가 플레이어 주도권을 없앨 수 있다. … 스왑을 다 써도 재미없으면 **이 아이디어는 버립니다.**"

게다가 그 완화책인 **스왑은 실제로 미구현이었다** — `RunData.m_SwapCount`를 소비하는 코드가
프로젝트 전체에 0곳이다(HUD 핍만 그려진다). 즉 플레이어 개입 수단이 현재 0개다.
이 추정의 반증은 베이스라인 QA에 포함해 Codex에 넘겼다.

## 확정된 설계 결정 (사용자)

| 갈림길 | 결정 |
|---|---|
| 유닛 수명 | 런 내내 누적. 전투에서 죽어도 다음 웨이브에 부활 |
| 판정 전력의 환산 | 1성만 지급. 승급은 3합으로만(자동 등급 전환 폐기) |
| 윷 | **예외로 둔다.** 업기·나기가 등급을 직접 만드는 종족 규칙이라 전력이 남아 올리는 자동 전환과 다르다 |

## 현재 상태 (2026-09-11 갱신)

stash는 **복원 완료**했고 저장소 밖으로 뺐던 `RunUnit.cs`·`RunRoster.cs`도 되돌렸다.
끊겨 있던 `InGameScene`의 전투 호출부도 이었다.

**컴파일 통과 — 에러 0 · 경고 0**(Codex가 전체 강제 Reimport 후 확인). 함께 확인된 것:

- `UIInGameBattle.Begin(RunRoster, ...)` 호출부는 `InGameScene.cs:207` 한 곳뿐
- `UIInGameField.ShowSummon` 참조 0건 — **씬 7개·프리팹 4개의 직렬화 `m_MethodName`에도 0건**
  (메서드 이름을 바꿨으니 UnityEvent가 끊겼을 수 있어 확인을 요청한 지점이었다)
- `MAX_SUMMON` 참조 0건, 2인자 `SummonSlot` 생성자 정의·호출 각각 0건

### ✅ 런타임 검증 통과 (2026-09-12, Codex Play Mode)

보고서: `D:/Orca/reports/roster-qa-2026-09-11.md`. 커밋은 `4632a8f`(main 미병합, 미푸시).

| 측정 | 결과 |
|---|---|
| 명부 누적 | ✅ 전력 11 → 지급 11. **전장 9 / 보관 6** — 9칸 초과분이 보관함에 남는다 |
| 비윷 즉시 고성급 | ✅ **0회** — 1성만 나온다(윷만 예외라는 설계대로) |
| 3합 승급 | ✅ 1성→2성, **연쇄 3성**까지. 물리 수 −2, 재료 등가 보존 |
| 승급체 자리 | ✅ 전장 칸 유지 |
| 전투 스탯 반영 | ✅ 1성 10/2 · 2성 26/5 · 3성 70/14 |
| 웨이브 넘어 생존 | ✅ 승리 후 명부 유지, **새 런에서 0기**(`Init` 작동) |
| 자동 배치 | ✅ `FillFieldFromBench` 결과와 실제 렌더 일치 |
| 추가 스핀 | ✅ 1회 클릭에 26G/코인2/구매0 → 1G/3/1. **명부 불변** |
| 콘솔 | ✅ 오류 0 · 경고 0 |

**내가 제기한 중복 구독 가설은 기각됐다.** Action 리스너 1/1/1, SPIN runtime invoker 1로 중복 없음.
원인은 `BaseScene`이 `OnSetup`을 `OnEnable`이 아니라 **`Start`에서** 부르기 때문이다(생애 1회).
내가 grep 줄번호로 함수 경계를 추측해 틀렸다.

### ⬜ 이번 QA에서 못 본 것 3건

1. **3성 3기가 모였을 때 멈추는가**(`maxGrade` cap) — 자연 발생을 기다렸으나 안 나왔다
2. **아군 전멸 후 부활** — 전멸하는 판이 안 나왔다
3. **연차 경계에서 명부 유지** — 연차를 못 넘겼다(런이 1~2연차에서 닫힘)

셋 다 "그 상황이 안 나와서" 못 본 것이다. 다음 QA에서는 **상황을 만들어** 확인해야 한다.

## 한 것

1. `RunUnit` — 런 동안 사는 유닛 하나(종류 + 성급). 참조로 다룬다(드래그 대상의 개체 동일성)
2. `RunRoster` — 보관함 + 전장 9칸을 한 주체가 소유. 3합 승급, 연쇄 승급, 이동·교환, 전장 전력 합,
   배치를 안 해도 전투가 성립하게 하는 `FillFieldFromBench()`. 상세는 `.claude/class/RunRoster.md`
3. `Judge.BuildSummon` — 자동 등급 전환 제거, 9칸 상한 제거, 전력만큼 1성 지급, `SymbolType` 항상 채움
4. `JudgeResult` — `MAX_SUMMON` 제거(미사용), 2인자 `SummonSlot` 생성자 제거, 머리 주석을 새 규칙으로 갱신
5. `RunData` — `RunRoster`를 들고 `Init()`에서 초기화
6. `InGameScene.OnSpinSettled` — 얻은 유닛을 명부에 넣는 `AddSpinUnits()` 연결
7. `UIInGameBattle.Begin`·`SpawnAllies` — 아군을 `JudgeResult`가 아니라 `RunRoster` 전장에서 세운다
8. `UIInGameField.ShowSummon` → `Show(RunRoster, JudgeResult, ...)` — 전장은 명부가 정본, 판정 요약은 스핀 1회의 것
9. 전투 시작과 스핀 직후 `FillFieldFromBench()` — 드래그 UI가 없는 동안의 자동 배치

## ⬜ 남은 것

1. ~~런타임 검증~~ → ✅ 통과. 다만 위 ⬜ 3건은 상황을 만들어 다시 봐야 한다
2. **보관함 UI + 드래그 앤 드롭** — 씬·프리팹 작업이라 Codex 몫. 아직 시작 안 했다.
   지금은 `FillFieldFromBench()`가 앞 칸부터 자동으로 채우는 것이 전부다
3. `SYMBOL_FROM_GRID` 정리 — 판정기가 이제 항상 채우므로 폴백 분기가 죽었다
4. **밸런스 재조정 — 방향 결정 대기.** 아래 베이스라인 절 참고
5. **GDD를 실제 구현 상태로 고쳐야 한다** — 「02 코어 루프 STEP 03 배치」와 「03 슬롯 시스템」이
   여전히 "릴 좌표 그대로 전장에 내려앉는다"로 적혀 있다. QA가 끝나면 고친다(지금은 파일 소유가 QA 쪽)

## 📌 기록해둘 것

- 이 작업 중 `Judge`·`InGameScene` 등을 건드리는 바람에, 같은 저장소에서 돌던 Codex의 베이스라인 QA와
  **파일 소유가 겹쳤다.** 브리프에는 "RunUnit/RunRoster만 건드린다"고 적어 보냈는데 작업이 그 위로 번졌다.
  Play Mode 진입 직전에 알아채 멈췄다. 브리프의 소유 범위는 작업이 커지면 **다시 알려야 한다.**

## 📊 베이스라인이 드러낸 것 (2026-09-11 QA, `D:/Orca/reports/baseline-qa-2026-09-11.md`)

**변경 전** 상태를 3회 완주로 실측했다. 예상과 달랐다.

| 런 | 종족 | 종료 | 사유 | HP |
|---|---|---|---|---|
| 1 | Poker | 2연차 3웨이브 | OutOfSpinCoin | 4 |
| 2 | Chess | 1연차 2웨이브 | OutOfSpinCoin | 8 |
| 3 | Chess | 1연차 2웨이브 | OutOfSpinCoin | 8 |

1. **진짜 병목은 적 강함이 아니라 스핀 코인이다.** 3회 모두 코인 소진으로 닫혔고 `homeHit`은 전 전투 0이다.
   12연차 설계인데 Chess는 1연차를 못 넘긴다.
2. **동수면 진다.** 적 전력 5에 판정 전력 5.0은 패, 6.5는 승. 1.2배는 돼야 이긴다.
   그런데 Chess 판정 전력은 3.9~6.5로 적 전력 5·6·7과 겹쳐, 1연차부터 구조적으로 진다.
   재도전이 코인을 먹고 코인이 마르면 런이 닫힌다 — 3회 모두의 사인이 이것이다.
3. **63 초과 전력 손실은 실전에서 0건이었다.** 관측된 최대 손실은 0.4다.
   이 작업의 이득으로 "63 초과분 손실이 사라진다"를 꼽았는데 **그건 정정해야 한다** —
   이론상 존재하지만 실전 영향이 거의 없다. 실질 이득은 **성장 축이 생기는 것** 하나다.
4. **이 변경은 단기적으로 전력을 낮춘다.** 전력 14는 예전엔 9칸에 등급을 올려 배치 전력 15를 냈지만
   이제 전장에 1성 9기(전력 9)만 선다. 누적이 돌면 2성 9기로 23.4까지 가지만,
   **베이스라인이 이미 1연차에서 죽고 있어 로스터만 넣으면 더 빨리 죽을 수 있다.**

### ⬜ 사용자 결정 대기 — 스핀 경제

로스터가 성장 축으로 작동하려면 누적할 시간이 필요한데 지금은 5회 스핀(3 + 추가 2) 안에 런이 끝난다.

| 후보 | 트레이드오프 |
|---|---|
| 패배 시 코인을 안 쓴다 | 재도전이 성장 기회가 된다. 대신 실패 비용이 사라져 긴장이 빠진다 |
| 연차당 코인을 늘린다(3 → 5~6) | 가장 단순. GDD 수치 변경 |
| 웨이브 진행을 승리 조건에서 뗀다 | 패배해도 다음 웨이브로 가고 HP만 깎인다. 구조 변경이 크다 |

GDD의 「패배하면 같은 웨이브 재도전」은 2026-09-10에 확정한 규칙이라 바꾸면 그 결정을 뒤집는 셈이다.

### 확인이 필요한 의심 1건

Chess 런 2·3이 패배 3~4회인데 종료 HP가 8이다. Poker는 8→4로 패배당 1씩 깎였다.
Chess 종족 보너스로 최대 HP가 12라면 12−4=8로 맞아떨어지므로 버그가 아닐 수 있다. 단정하지 않는다.

## 2026-09-11 세션 종료 시점 정리

### 반영한 밸런스 변경

`GameConfigTable.csv`의 `SpinCoinPerYear` **3 → 5**(사용자 지시). 추가 스핀 2회를 합해 실질 7회다.
베이스라인에서 실질 5회로 1연차를 못 넘긴 실측이 근거다.

> 어제(2026-09-10) 같은 값을 3→5로 올렸다가 되돌린 이력이 있다. 그때는 추가 스핀이 없어
> "기본 3 + 추가 2 = 5"와 중복이라는 이유였고, 지금은 그 실질 5회로도 부족하다는 실측이 있다. 모순이 아니다.

### GDD를 실제 구현 상태로 맞췄다 (12곳)

`Assets/Design/reel_of_four_houses_GDD.html`

| 절 | 무엇을 |
|---|---|
| 고유 축 · 03 슬롯 시스템 | "릴 좌표 = 전장 좌표"를 취소선 + 폐기 사유로 |
| 02 코어 루프 STEP 01·03 | 코인 3→5, 배치를 "보관함에서 드래그"로 |
| SpinEconomy · 판돈 절 | 기본 스핀 5, 올린 근거 병기 |
| 07 전투와 배치 | 릴 1:1 대응 폐기, 열별 역할이 **미구현**임을 명시 |
| 플레이어 개입 지점 | 무료 스왑 2회 → 드래그 배치. **스왑이 끝까지 미구현이었다는 사실** 기록 |
| 08 UnitGrowth | 흡수가 유일한 승급 경로가 됐음, 연쇄, 윷 예외, 각인·프리즘 미구현 |
| 13 치명 리스크 1번 | **"실현됐고 위 문장대로 버렸다"**는 결과를 추가 |

13장 리스크에 결과를 남긴 게 핵심이다 — 기획서가 미리 적어둔 탈출구가 실제로 탈출구가 됐다는 기록이다.

### ⬜ 스왑 잔재 정리가 남았다

`RunData.m_SwapCount`·`m_SwapCountMax`, `GameConfigTable.KEY_SWAP_COUNT_PER_YEAR`,
`UIInGameAction`의 스왑 핍·라벨이 그대로 있다. 드래그 배치가 그 역할을 대신하므로 정리 대상이지만,
지금 지우면 HUD/ACTION 표시가 깨진다. **드래그 UI를 붙일 때 함께 정리한다.**
(미완료 항목 중 `SwapText` 폭 여유 0 결함도 이때 같이 사라진다)

### 미커밋 상태

이 작업 전체가 아직 커밋되지 않았다. Git 마무리는 Codex 메인 세션 몫인데 **사용량이 소진**됐다.
다음 세션에서 Codex가 돌아오면 인계하거나, 사용자가 그 건에 한해 위임하면 처리한다.

## 📊 베이스라인 대조 (2026-09-12)

| 종족 | 변경 전 | 변경 후 | 비고 |
|---|---|---|---|
| Poker | 2연차 3웨이브 · HP4 | **1연차 3웨이브 · HP8** | 연차는 뒤로, HP는 더 남음 |
| Chess | 1연차 2웨이브 · HP8 | **1연차 2웨이브 · HP11** | 같은 지점, HP 더 남음 |

**둘 다 여전히 `OutOfSpinCoin`으로 닫힌다.** 스핀 코인을 3→5로 올렸는데도 그렇다.

해석(표본이 각 1회라 확정 아님):

- **HP가 더 남은 것**은 코인이 늘어 재도전 여유가 생겼거나 전투를 덜 졌다는 뜻이다
- **Poker가 2-3 → 1-3으로 뒤로 간 것**은 예측했던 **초반 전력 저하**가 실재한다는 신호다.
  전력 14는 예전에 등급 전환으로 배치 전력 15를 냈지만 이제 전장 9칸에 1성 9기(전력 9)만 선다.
  누적이 도는 2~3연차 전에 런이 끝나면 성장 축이 작동할 기회 자체가 없다

### ⬜ 밸런스 손볼 후보

1. **1성 기본 스탯을 올린다** — 가장 단순. 다만 적 곡선과 함께 봐야 한다
2. **적 곡선 초반을 낮춘다** — 1~2연차를 넘기게 해 누적이 돌 시간을 준다
3. **전장 칸을 늘린다** — 9칸 상한이 초반 전력을 묶고 있다. 다만 UI·배치 전반에 영향이 크다
4. **승급 문턱을 낮춘다** — 3기가 아니라 2기로. 승급이 빨라져 초반 전력이 회복된다.
   GDD의 "동일 유닛 3기 흡수"를 바꾸는 것이라 기획 판단이 필요하다

**표본을 늘리는 게 먼저다.** 종족당 3회 이상 돌려 평균 종료 연차를 봐야 방향을 정할 수 있다.

## 2026-09-12 세션 종료 (사용자 지시로 중단)

### 미커밋 상태 — 전부 디스크에 있고 유실 없음

| 파일 | 무엇 |
|---|---|
| `Assets/Resources/Table/WaveTable.csv` | **1-1 `PowerCoef` 0.85 → 0.6** (grunt 5 → 4마리). 유일한 코드/테이블 변경 |
| `.claude/design/unit-spec.html` | 기획 에이전트가 낸 유닛 스펙(126KB) |
| `.claude/architecture/ingame-presentation.md` | 드래그 배치 · 전투 후 제자리 복귀 · 연차 이동 연출 설계 |
| `.claude/architecture/unit-table.md` | 레인 제약 없음으로 정정 |
| `.claude/briefs/2026-09-12-balance-sample.md` | 밸런스 표본 측정 브리프(중단됨) |

커밋 `4632a8f`(유닛 명부)까지는 이미 들어가 있다. 위 변경은 그 위에 쌓인 미커밋분이다.

### ⬜ 중단된 것 — 밸런스 표본 측정

Codex가 원칙 A(웨이브당 스핀 1회)로 돌리던 중 중단했다. 부분 결과는
`D:/Orca/reports/balance-sample-2026-09-12.md`에 있을 수 있다.

**주의**: 중단 직전 Poker 6연차 도달 관측이 있었으나 **자동화 상태기 결함으로 정식 표본에서 제외**됐다
(코인 0 처리에서 NOCHANGE 반복). 다음에 재측정해야 한다.
다만 **연차 경계에서 코인 5 리셋 + 명부 유지**는 그 런에서 관찰 증거로 확보했다.

### ★ 다음 세션에서 가장 먼저 볼 것 — 자동 배치가 병목이다

시뮬레이션(`scratchpad/merge_rate.py`, 심볼 균등 가정)에서 나온 것:

| 6연차 3웨이브 시점 전장 전력 | 심볼 6종 | 9종 | 13종 |
|---|---:|---:|---:|
| 최선 배치(강한 9기) | 62.5 | 59.5 | 49.0 |
| **자동 배치(현재)** | 37.9 | 31.7 | **25.0** |

**같은 보유량인데 배치만 바꿔도 거의 2배다.** 실측 Poker 6-3의 전장 전력 30.6도 자동 쪽에 가깝다.

원인은 `RunRoster.FillFieldFromBench()`가 **보관함 앞쪽부터** 채우는 것이다 —
강한 유닛을 고르지 않아 3성이 보관함에 남고 1성이 전장에 서는 일이 생긴다.

**→ 가장 싸고 효과 큰 수정: `FillFieldFromBench()`가 성급 높은 것부터 채우게 한다.**
드래그 UI 없이도 자동 배치가 최선에 근접한다.

그다음이 드래그 배치, 그다음이 유닛 테이블 1단계(원거리)다.
전력 30.6 대 적 24.15로 **1.27배 우위인데 진** 것은 6연차 적이 archer(사거리 3)이기 때문으로 보인다 —
근접 아군은 접근하는 동안 일방적으로 맞는다. 아군에게도 원거리를 주는 것이 그 답이다.

### ⬜ 답이 필요한 기획 질문 2건 (기획 스펙 Q3·Q4)

1. **`Atk`를 float로 승격할지** — 1성 Atk가 정수 2라 포커 13단 사다리가 1성에서 3단으로 뭉갠다
2. **화투 죽은 패(11·12월)를 최강으로 둘지** — GDD와 어긋난다. 채택하면 GDD도 함께 고쳐야 한다

## 2026-09-13 — 유닛 테이블 1단계 구현 (미커밋 · **Play Mode 검증 통과**)

상세는 `.claude/class/UnitRecord.md`. 기획 출처는 `.claude/design/unit-spec.html`.

### 미커밋 파일 (디스크에 있고 유실 없음)

| 파일 | 무엇 |
|---|---|
| `Assets/Resources/Table/UnitTable.csv` | **신규 59행** — 7종족 전 심볼의 배율·Role·RangeBonus·TraitKey |
| `Assets/Scripts/Table/UnitRecord.cs` | **신규** — `UnitRecord`, `UnitBattleStat` struct, `UnitTable.GetBattleStat()` |
| `Glory/Table/TableManager.cs` | `UnitTable` 등록 |
| `Table/UnitGradeRecord.cs` · `Table/EnemyRecord.cs` | `Atk` int → **float** |
| `InGame/Battle/BattleUnit.cs` | `m_Atk`·`Setup`·`TakeDamage`, `m_Hp`·`m_MaxHp`까지 **float** |
| `InGame/Battle/UIInGameBattle.cs` | `SpawnAllies`가 `GetBattleStat` 사용, `Begin`에 `houseKey` 인자. **틀린 클래스 주석 정정** |
| `InGame/InGameScene.cs` · `InGame/RunData.cs` | 종족 키 전달(`RunData.houseKey` 신규) |

### 검증 완료 (2026-09-13) — 네 번 막혔던 항목이 닫혔다

**컴파일**: Claude Code가 브릿지 없이 확정했다. `Assembly-CSharp.dll` 빌드 시각 11:31:40이
최신 소스 11:02:19보다 뒤이고, `Editor.log`에 `error CS` 0건이며, DLL 메타데이터에
`UnitRecord` · `UnitBattleStat` · `UnitTable` · `GetBattleStat`이 모두 있다.
방법은 `D:/Orca/runbook/unity-bridge.md`에 남겼다.

**Play Mode 2~4순위**: Codex가 새 세션(브릿지 연결됨)에서 전부 통과시켰다.
보고서 `D:/Orca/reports/unit-table-qa-2026-09-13.md`.

| 항목 | 결과 |
|---|---|
| 심볼별 스탯 차이 | slot 1성 cherry `HP12/ATK1.5`, seven `HP7/ATK2.9` — 실제 `BattleUnit`까지 차이 유지 |
| 전투 종료 | poker 실제 풀·테이블 13종으로 1-1~1-3 모두 Victory(4.14 / 4.68 / 4.90초), 타임아웃 0 |
| `ATK_MIN` 하한 | 합성 `AtkRate=0`에서도 ATK 1 유지 |
| 소수 피해 누적 | 0.4씩 3회에 `1 → 0.6 → 0.200000018 → 0`, 사망 판정까지 도달 |
| HP바 | `10/12 = 0.8333333`이 `fillAmount`와 일치 |
| 없는 심볼 폴백 | `slot/999`가 성급 원값 `HP10/ATK2/Spd1/Range1/Move1`, 오류 로그 정확히 1건(중복 없음) |

**Claude Code가 원본에서 재확인한 것** — cherry `1.25×10=12.5→12` · `0.75×2=1.5`,
seven `0.70×10=7` · `1.45×2=2.9`로 CSV와 산술이 정확히 맞는다.
또 QA 콘솔에 남은 `slot/6~8` 오류가 생산 경로 결함인지 확인했다: `InGameScene.cs:64`와
`RunData.cs:69`가 **둘 다** `PlayerManager.GetSelectedHouseRecord()`를 읽으므로
`houseKey`와 스프라이트 풀은 같은 출처에서 파생된다 — Codex의 "QA 입력 불일치" 판정이 맞다.

**남은 것은 Git 마무리뿐이다**(Codex 메인 세션 몫). 커밋 시 폰트 아틀라스 4개는 제외한다.

### 검산한 것

CSV를 만든 뒤 직접 계산해 확인했다 — 7종족 모두 `Hp`·`Atk`·`Spd`·`Mv` 배율 평균 **1.000**,
`Id` 1~59 중복 없음. 다만 **심볼 균등 가중**이라, 판정이 특정 심볼을 과대 대표하는 것을 감안하면
실제 획득 분포로는 1.0이 아닐 수 있다(기획 스펙 Q8).

### 확정된 기획 결정 (2026-09-12 사용자)

- **Q4 `Atk` float 승격** — 채택. 정수 2에 배율 0.55~1.5를 곱하면 1·2·3으로 뭉개져
  런 초반(전부 1성)에 "유닛이 다르다"가 가장 안 보였다
- **Q3 화투 죽은 패** — GDD 대로 둔다. 배율로 되살리면 코어가 붙을 때 부활 경로가 이중이 된다

### ⬜ 이 단계에서 일부러 안 한 것

- `StringTable`에 `NameKey` 59행 — 유닛 이름을 쓰는 UI가 아직 없어 지금은 무해하다
- `Role`·`TraitKey`는 **저장만 하고 전투가 읽지 않는다**(2·3단계)
- `UnitSpawnTable`(소환형) — 5단계

---

## 2026-09-13 — 환경: Unity 자동 실행을 넣었다 (Orca 쪽)

Unity MCP가 **세션 시작 때 한 번만** 붙는 탓에 2026-09-11~13에 QA가 네 번 막혔다.
세션 안에서는 못 푸는 문제라 환경 쪽에서 막았다(사용자 지시).

| 파일 | 역할 |
|---|---|
| `D:\Orca\StartUnityOnLogon.ps1` | Unity Hub 목록에서 **가장 최근 프로젝트**를 골라 기존 런처에 넘긴다. `-DelaySeconds` |
| `%APPDATA%\...\Startup\Orca-StartUnity.vbs` | 로그온 트리거. 창 숨기고 **120초 지연** |

작업 스케줄러(`schtasks /sc onlogon`)는 `Access is denied`로 막혀 시작 폴더를 썼다 —
로그온 트리거 등록은 관리자 권한을 요구한다.

### ⬜ 첫 실전 검증 지점 (다음 부팅)

지금까지는 Unity가 이미 떠 있어서 **"건너뛰기" 경로만** 확인했다. 실제 실행 경로는 미검증이다.

**재부팅 후 확인할 것** — 세션 시작 표의 `Unity MCP` 줄이 **`OK 브릿지 응답 (8080)`**이면 성공이다.
`⚠ 브릿지 없음`이면 자동 실행이 안 된 것이니 시작 폴더의 vbs와 로그를 본다.
(로그온 후 120초 지연이 있으므로, 부팅 직후 바로 세션을 열면 아직 안 떠 있을 수 있다)

---

## 2026-09-13 오후 — 포커 하방 진단 (계산 완료 · 실측 대기)

밸런스 표본에서 **Poker 1-3 / Chess 12-1 / Yut 12-3**이 나왔고, 그 격차의 원인을 계산으로 닫았다.
전체는 `.claude/briefs/2026-09-13-poker-variance.md`.

### 확정된 것

- 포커 고분산은 **GDD 05장이 의도한 설계**다. 실제 판정 로직 20만 회가 GDD 50만 회 표와
  소수점까지 일치했다(chess 6.11/3.90/0.64, poker 6.27/10.15/1.62). 구현이 기획을 정확히 옮겼다.
- **포커 1연차 종료는 32.6% 확률의 사건이고 chess는 0.76%다.** 운이지만 43배 더 자주 걸린다.
- 추가 스핀(골드 25)이 안전망인데 **골드가 판정 전력 비례라 포커는 못 산다.**
  실측 최종 골드 20이 계산과 맞았다.

### GDD 갱신 완료 (2026-09-13)

`Assets/Design/reel_of_four_houses_GDD.html` 두 곳을 `class="drop"` 정정 단락으로 닫았다.

**05장 「넷 — 포커의 무소환 8.9%는 그대로 둔다」** — 완충 논증이 잘못된 지표를 썼다는 것과,
원인이 계수가 아니라 구제 수단의 가격이었다는 것, 병목이 횟수 상한으로 옮겨간 것을 적었다.

**12장 게이트 「아무 코어 없이 12연차 클리어율 20~35%」** — **미통과**를 명시했다.
도달은 포커 6/8 · 체스 2/2인데 **클리어는 0/10**이다. 다만 게이트가 전제한 플레이어 개입 수단
(스왑·드래그 배치)이 아직 없으므로 그것을 넣은 뒤 재측정하는 것이 순서라고 함께 적었다.

### 적용한 조정 — 검증 완료

| 파일 | 변경 | 근거 |
|---|---|---|
| `GameConfigTable.csv` | `RunStartGold` 0 → **25** | 포커 1연차 종료 **8런 중 0회**, 귀무가설 기준 **p=0.043** |

실측 8런: 5-3 / 11-3 / 12-3 / 12-3 / 12-3 / 12-2 / 12-3 / 12-2.
체스 2런(12-2 / 12-3)으로 상방 붕괴가 없음을 확인했다.
보고서 `D:/Orca/reports/poker-variance-2026-09-13.md`.

**왜 이 한 줄이 듣는가** — 시작 골드가 0이면 1연차에는 추가 스핀을 살 방법이 원리적으로 없다.
골드를 먼저 벌어야 하고, 골드는 판정 전력 비례라 저전력 구간에서는 벌리지 않는다.
그 1연차가 포커가 죽는 구간이었다. 실측 8런 전부에서 추가 스핀을 2회 샀다.

### ⬜ 다음 조정 후보 — 횟수 상한

가격 병목이 닫히자 **`ExtraSpinMaxPerYear=2`**가 새 병목이 됐다.
실측에서 종료 시 골드가 **585~663** 남았는데도 상한에 걸려 끝났다.
다만 이것은 12연차 클리어 문제와 얽혀 있고, 위 GDD 12장 기록대로 **개입 수단 구현이 먼저다.**

### ⬜ 별건 — 전력 지표가 실제 전투력을 과소평가한다

전장 9칸 최대 전력은 **63.0**(3성 9기)인데 10-3 적 목표 전력이 69, 12-3은 110.4다.
그런데 실측 Chess는 12-1(적 81.6)까지 갔다. 3성 1기(`Hp70/Atk14`)와 grunt 7마리(각 `Hp10/Atk2`)는
전력이 같은 7이지만 **동시 교전 수가 제한되므로 집중된 스탯이 이긴다.**

즉 `WaveTable`의 **「PowerCoef → 마릿수 역산」계약이 실제 난이도를 반영하지 못한다.**
같은 목표 전력을 1마리로 주는지 7마리로 주는지가 체감을 가른다. 이번 범위가 아니라 손대지 않았다.

**이 사실 때문에 전투를 전력 비교로 근사하면 안 된다** — Claude가 그 근사로 런 시뮬레이션을 만들었고
10연차에서 막히는 잘못된 결과를 냈다. 전투는 실제 `UIInGameBattle`을 돌려야 한다.

---

## 2026-09-13 저녁 — 드래그 배치 (검증 통과 · 미커밋)

브랜치 `work/2026-09-13-drag-placement`(`work/2026-09-13-poker-variance` 위 스택).
설계 `.claude/architecture/ingame-presentation.md` §1 · 브리프 `.claude/briefs/2026-09-13-drag-placement-qa.md`
보고서 `D:/Orca/reports/drag-placement-qa-2026-09-13.md`

### 무엇을 했나

플레이어가 **전장 9칸의 유닛을 드래그로 맞바꾼다.** 보관함 UI는 범위에서 뺐다.

| 파일 | 변경 |
|---|---|
| `RunRoster.cs` | `ArrangeFieldByGrade()` → **`FillEmptyFieldFromBench()`**(빈 칸만) + `FindHighestGradeBenchIndex()` |
| `UIInGameFieldSlot.cs` | 드래그 인터페이스 4개, `Setup(owner, cell)`, raycast용 Image 자가 부착 |
| `UIInGameField.cs` | 명부·풀 캐시, `RefreshSlots()`, 드래그 중재 4개 |
| `InGameScene.cs` | 호출부 2곳 |

**선행 조건이 함께 풀렸다** — 옛 자동 배치가 매 스핀 9칸을 다시 세워 플레이어 배치를 덮어썼다.
그 함수 주석의 제거 조건("드래그 핸들러가 붙는 시점")이 충족돼 좁혔다.

### 검증 통과 (Codex Play Mode)

| 항목 | 결과 |
|---|---|
| 컴파일 | DLL 19:28:50 > 소스 19:27:52, Error/Warning 0 |
| 점유칸 교환 · 빈칸 이동 | 통과 |
| **명부 실제 반영** | `OnBattleStart`에서 바꾼 배치대로 `BattleUnit` 생성. 실측 `cell0 cherry@(60,104)` 등 |
| **자동 배치가 덮지 않음** | 다음 `OnSpinSettled`에서 기존 배치 유지 + 새 유닛만 빈 칸 |
| 부정 경로 | 전투 중 · 빈 칸 · 칸 밖 · 같은 칸 모두 통과 |
| sibling 복원 | `[0:1 … 8:9]` 정상, `Clear()` 후에도 유지 |

**QA가 잡은 결함 1건을 고쳤다** — `SetAsLastSibling()`을 되돌리지 않아 연속 드래그 뒤
그리기 순서가 흐트러졌다(뒤 레인이 앞 레인 위에 그려짐). `m_SiblingHomeIndex`로 기억·복원.

### ⬜ 사람이 확인해야 하는 것 — 실제 포인터 입력

QA는 **MCP 한계로 OS 포인터 raycast hit-test를 건너뛰고** `PointerEventData` 핸들러와
중재 API를 직접 호출했다. 즉 **핸들러 로직·명부 반영은 검증됐지만, 실제 마우스로 끌었을 때
포인터가 잡히는지는 미검증이다.**

칸에 `AddComponent<Image>()`(alpha 0)로 붙인 투명 이미지가 입력을 받아야 한다.
**안 잡히면** 씬의 `SlotTemplate`에 Image를 넣는 씬 작업(Codex 몫)으로 넘어간다.

### ⬜ 이번에 일부러 안 한 것

- **보관함 UI** — `RunRoster.MoveBenchToField()` · `MoveFieldToBench()`는 여전히 호출부 0
- **스왑 잔재 정리**(`RunData.m_SwapCount`·`GameConfigTable.KEY_SWAP_COUNT_PER_YEAR`·
  `UIInGameAction` 스왑 핍) — 설계안은 "함께 걷어낸다"고 했지만 드래그 검증이 먼저였다.
  같이 건드리면 무엇이 깨졌는지 가릴 수 없다. **이제 검증이 끝났으니 다음에 한다**
- `RunRoster.GetFieldPower()`는 **원래부터 호출부 0건**이다(미사용). 내 변경과 무관해 두었다

---

## 2026-09-14 — 후반 곡선 완화 (기획 관문 통과 · 미커밋)

브랜치 `work/2026-09-14-late-curve`(`work/2026-09-13-year-travel` 위 스택).
브리프 `.claude/briefs/2026-09-14-balance-remeasure.md` · `2026-09-14-late-curve.md`
보고서 `D:/Orca/reports/balance-remeasure-2026-09-14.md` · `late-curve-2026-09-14.md`

### 결과

**기획서 12장 관문을 통과했다** — 체스 8런 중 **2런 클리어(25.0%)**, 목표 20~35%.

| 파일 | 변경 |
|---|---|
| `EnemyTable.csv` | `king_final` Atk 78 → **55**, Hp 1650 → **850**. `Power 110`은 불변 |
| `WaveTable.csv` | 12-2 `PowerCoef` 16.0 → **14.0** |

### 진단이 두 번 뒤집혔다

1. **관문이 체스 기준이었다.** 기획서 12장 앞 줄이 "단일 종족(체스)만으로 완주 가능하게"다.
   2026-09-13에 GDD에 적은 "클리어 0/10"은 포커·체스를 섞은 수치라 이 관문의 지표가 아니었다.
2. **`PowerCoef`로는 12-3을 못 고친다.** `GetSpawnCount`의 `Max(1, …)` 때문에 보스 마릿수가
   1에 고정이다. 후반 완화를 계수로만 하면 마지막 웨이브가 그대로 남는다.

결정적 사실은 `king_final.Atk 78 > 아군 3성 Hp 70` — **한 방에 죽었다.**
9초 전멸 동안 누적 피해 630 대 보스 HP 1650으로 **만렙으로도 산술적으로 불가능**했다.

### ⬜ 검증 못 한 것 · 남은 것

- **표본 8런이라 12.5% 단위로만 측정된다.** 25%는 그 눈금 위의 값이다
- **사람이 직접 플레이한 결과가 아니다.** 전부 자동 배치이고, 드래그로 배치를 고르면 달라진다
- **열 배치 A/B 측정은 실패했다** — 배치가 승패를 바꾸면 스핀 횟수가 달라져 전역 난수 소비가
  갈리고 이후 그리드가 다른 런이 된다. 제대로 재려면 **판정 난수를 분리하거나 그리드를 주입**해야
  한다(프로덕션 변경). 별건
- **Yut의 보스 피해량이 체스의 절반**(429~520 vs 822~960). 종족 밸런스 문제, 미착수
- **Poker는 보스에 도달하지 못한다.** 1-3 코인 소진 런이 다시 나왔다 —
  `RunStartGold 25`의 p=0.043은 "유의하게 감소"였지 0이 된 것이 아니다
- **보스 도달 실패 3/8은 의도적으로 안 건드렸다.** 추가 스핀 상한을 올리면 도달률이 오르지만
  "코인이 곧 실패 허용 횟수"가 이 게임의 긴장 구조다
