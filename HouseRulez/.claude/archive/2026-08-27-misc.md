# 완료 기록 — 2026-08-27~28 (기획 HTML 정본화 · 릴 검증) + 작업트리 정리

> 완료·커밋된 과거 기록. `.claude/UNFINISHED.md`에서 분할됐다(2026-09-10, 200줄 규칙).
> 각 시스템의 **최신 상태는 `.claude/class/` · `.claude/prefab/` 문서가 정본**이다.

---

## 2026-08-28-4 — 기획 문서 HTML 정본 전환

완료. `.claude/design/`의 기존 기획 Markdown 5개를 HTML로 교체하고 Orca 정본의 `upgrade-system.html`을 포함해 총 6개 문서를 동기화했다. 문서 간 `.html` 링크, `design-planner` 산출물 형식, `code-writer` 입력 경로도 함께 갱신했다.

## 2026-08-27-0 — 릴 실행 검증 완료, 사람 눈 확인만 남음 (브랜치 4개)

MCP가 붙어 **Play Mode로 처음 실제 검증**했다. 어제 미검증으로 남긴 항목 대부분이 여기서 해소됐고,
동시에 정적 대조로는 안 잡히던 이식 결함 6건이 드러나 오늘 전부 고쳤다(브랜치 3).

~~**네 브랜치 모두 `main` 합류 전이다.**~~ → **2026-09-09 정정: 네 브랜치 모두 `main`에 합류했고 삭제됐다.**
당시엔 Play Mode까지 돌려보고 사람 눈 확인만 남은 상태였다(브랜치 4는 2026-08-27 후속 세션에서 8항목 실측 완료).

### 오늘 배운 것 — 정적 대조는 실행 검증을 대체하지 못한다
어제 참조 정합성 / 코드 규칙 / 씬 YAML 무결성을 전부 통과시켰는데, 오늘 드러난 6건 중
그 검사로 잡히는 건 **하나도 없었다.** 전부 컴파일도 되고 실행도 되는데 화면만 이상한 종류다.
검사 항목을 늘려서 될 일이 아니라 한 번 돌려보는 것 말고는 방법이 없는 구간이 있다.
특히 **어제 "가장 위험하다"고 적어둔 `RectMask2D`가 실제로 안 붙어 있었다** — 위험하다고 적어둔 것과
실제로 확인한 것은 다르다.

---

> 📦 **아래 「브랜치 1~5」는 과거 기록이다.** 다섯 브랜치 모두 로컬·원격에서 **이미 삭제됐고**
> 내용은 `main`에 녹아 있다(2026-09-09 실측). 남긴 이유는 당시 판단 근거와 미해결 항목 때문이다 —
> 브랜치를 다시 찾으려 하지 말 것.

## ~~커밋하지 않고 남겨둔 것 (작업 트리)~~ — 2026-09-09 해소됨

- ~~`Assets/DefaultVolumeProfile.asset`~~ — **커밋됐다.** 작업 트리에 변경 없음
- ~~`Assets/Screenshots/` (14장)~~ — **커밋돼 있다**(현재 32개 파일, 최초 `7b54b5b`).
  ⚠️ 지우거나 `.gitignore`에 넣자는 판단은 **실행되지 않았고, 저장소에 그대로 남아 있다.**
  QA 캡처는 이후 `QACapture/`로 옮겨 무시 처리했으므로, 이 폴더는 정리 대상으로 남아 있다

현재 작업 트리의 미커밋 변경은 맨 위 「📍 현재 저장소 상태」의 2건뿐이다.

---

