// 접이식 요약 패널이 보여주는 것. 버튼 하나로 이 순서대로 돌아간다.
//
// 값의 순서가 곧 순환 순서라 **중간에 끼워 넣으면 흐름이 바뀐다.**
// 새 탭은 뒤에 붙인다.
public enum eSummaryTab
{
    // 이번 스핀이 왜 이만큼 나왔는가.
    Result = 0,

    // 이 종족은 무엇이 걸리면 얼마인가.
    Pattern,

    // 지금 전장에 선 유닛은 무엇인가.
    Unit,
}
